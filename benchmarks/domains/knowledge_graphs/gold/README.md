# Gold por pack

- `../gold.json` — seed histórico (`pack_seed`); probes actuales **saturantes** (muchas D0/trivia).  
- `gold_pack_kg_multi_v1.json` — **will be (E1):** probes `from_text`, spans, D1–D5, `docs_required`.

No promover probes D0 al set de evaluación. Ver EXPERIMENTAL_DESIGN §4 y §10 E1.
