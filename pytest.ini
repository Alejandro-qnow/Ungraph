[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks tests as integration tests (requires Neo4j)
    unit: marks tests as unit tests (no external dependencies)
    e2e: marks tests as end-to-end tests (requires Neo4j)
    eval: marks tests that need DeepEval and/or LLM judge API keys
    openai: marks tests that require UNGRAPH_OPENAI_API_KEY (or compatible)
addopts = 
    -v
    --strict-markers
    --tb=short

